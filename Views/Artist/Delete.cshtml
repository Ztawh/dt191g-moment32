@model dt191g_moment32.Models.Artist

@{
    ViewData["Title"] = "Radera";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Radera</h2>

<h3 class="border-bottom">Är du säker på att du vill radera den här artisten?</h3>
<div>
    <h4>Artist</h4>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    
    <form asp-action="Delete" class="border-top">
        <input type="hidden" asp-for="ArtistId" />
        <span class="link delete-wrapper"><input type="submit" value="Radera" class="btn-delete" /></span> |
        <a class="link" asp-action="Index">Tillbaka</a>
    </form>
</div>
